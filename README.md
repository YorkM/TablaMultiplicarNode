# Notas:

Este es mi primer programa en Node

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Toma el valor de la base para crear la tabla de multiplicar
                                                             [number] [required]
  -h, --hasta    Es el valor del limite de la tabla de multiplicar
                                                             [number] [required]
  -l, --listar   Muestra la tabla en consola          [boolean] [default: false]

Missing required arguments: b, h

```